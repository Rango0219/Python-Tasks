# Python-Tasks
# Task - 1
Description:
This program takes two numbers as input from the user.
Basic mathematical operations are performed on these two numbers.
I have also added that after performing the operation
if user presses any key it will exit the program.

#Task - 2
This program takes user`s First name and Last name as input.
 Concatenates both the name and a greeting messsage is being printed.
 I have also added that after performing the operation
if user presses any key it will exit the program.


